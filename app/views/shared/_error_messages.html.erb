<% if @applicationForm.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-danger">
      The form contains <%= pluralize(@applicationForm.errors.count, "error") %>.
    </div>
    <ul>
    <% @applicationForm.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>